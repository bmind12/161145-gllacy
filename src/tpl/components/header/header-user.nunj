<ul class="user-panel">
    {% for name, link, modifier in user %}
        <li class="user-panel__item">
            <a class="user-panel__link {{modifier}}" href="{{link}}">{{name}}</a>
        </li>
    {% endfor %}
</ul>
